<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="icon" href="images/g.png" type="image/x-icon">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #f0f0f0;
            background-image: url('../images/nairobi.jpg'); no-repeat center;
            background-repeat: no-repeat;
            background-size:cover;

        }

        /* Custom CSS for rounded container */
        .rounded-container {
            border: 1px solid #e5e5e5;
            border-radius: 15px; /* Adjust this value for the level of rounding you desire */
            padding: 20px;
        }
        /* Custom CSS for container style */
        .custom-container {
            border: 1px solid #e5e5e5;
            border-radius: 15px; /* Adjust this value for the level of rounding you desire */
            padding: 20px;
            max-width: 400px; /* Adjust the width as needed */
            margin: 0 auto; /* Center the container horizontally */
        }
	.border {
	    max-width: 400px;
            margin: 18px auto;
 	    background-color: coral;
            padding: 13px;
	    border-radius: 15px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
	}
	.card-body {
	    background-color: coral;
	}
    </style>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
</head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <!-- Add rounded corners and a border to the container -->
                <div class="border rounded p-4">
                    <div class="text-center">
                        <!-- Login with social media buttons (replace 'A', 'G', 'T' with respective logos) -->
                        <h2>Login via</h2>
                        <a href="/auth/facebook" class="btn btn-primary btn-lg m-2" ><i class="bi bi-facebook"></i></a> <!-- Replace 'A' with Facebook logo -->
                        <a href="/auth/google" class="btn btn-danger btn-lg m-2"><i class="bi bi-google"></i></a> <!-- Replace 'G' with Google logo -->
                        <a href="/auth/twitter" class="btn btn-info btn-lg m-2"><i class="bi bi-twitter-x"></i></a> <!-- Replace 'T' with Twitter logo -->
                    </div>

                    <!-- Line separating social media login and email/password login -->
                    <div class="text-center my-3">
                        <!-- <hr class="w-25 m-0"> -->
                        <span class="mx-2">or login below...</span>
                        <!-- <hr class="w-25 m-0"> -->
                    </div>

                    <div class="card">
                        <!-- <div class="card-header">Login</div> -->
                        <div class="card-body">
                            <form action="/login" method="POST">
                                <div class="mb-3">
                                    <label for="username" class="form-label">Username</label>
                                    <input type="text" class="form-control" name="username"  id="username" placeholder="Enter your username" required>
                                </div>
                                <div class="mb-3">
                                    <label for="password" class="form-label">Password</label>
                                    <input type="password" class="form-control" name="password" id="password" placeholder="Enter your password" required>
                                </div>
                                <button type="submit" class="btn btn-primary">Login</button>
                            </form>
			    
                        </div>
			<% if (message) { %>
			 <p class="message"><%= message %></p>
		        <% } %>
                    </div>
                    
                    <!-- Link to sign up -->
                    <p class="mt-3">Don't have an account? <a href="/api/signup">Sign up</a></p>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

